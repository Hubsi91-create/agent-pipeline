# ============================================================
# 11-Agent Music Video Production System - Environment Variables
# ============================================================

# ============================================================
# APPLICATION SETTINGS
# ============================================================

APP_NAME="Music Agents v2 - 11-Agent System"
APP_VERSION="2.0.0"
ENVIRONMENT=development
DEBUG=false

# ============================================================
# GOOGLE CLOUD SETTINGS
# ============================================================

# GCP Project Configuration
GOOGLE_PROJECT_ID=music-agents-prod
GOOGLE_REGION=global

# Google Sheets Database
# Get your Google Sheet ID from the URL:
# https://docs.google.com/spreadsheets/d/{SHEET_ID}/edit
GOOGLE_SHEET_ID=YOUR_SHEET_ID_HERE

# For local testing only (not needed on Cloud Run with ADC)
# Paste the entire JSON content of your service account key file
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# ============================================================
# API SETTINGS
# ============================================================

API_V1_PREFIX=/api/v1
HOST=0.0.0.0
PORT=8080

# CORS Origins (comma-separated for production)
# For development: ["*"]
# For production: ["https://yourdomain.com", "https://app.yourdomain.com"]
CORS_ORIGINS=["*"]

# ============================================================
# LOGGING SETTINGS
# ============================================================

# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================================
# AGENT CONFIGURATION
# ============================================================

# Agent 1: Trend Detective
AGENT1_DEFAULT_TREND_COUNT=5

# Agent 2: Suno Prompt Generator
AGENT2_DEFAULT_PROMPTS_PER_TREND=10

# QC Processor
QC_APPROVAL_THRESHOLD=7
QC_FEW_SHOT_EXAMPLE_LIMIT=15

# ============================================================
# GEMINI / LLM SETTINGS
# ============================================================

GEMINI_MODEL=gemini-2.5-pro
GEMINI_DEFAULT_TEMPERATURE=0.8
GEMINI_DEFAULT_MAX_TOKENS=8192

# ============================================================
# DEPLOYMENT NOTES
# ============================================================

# For Google Cloud Run:
# 1. Set GOOGLE_SHEET_ID in Cloud Run environment variables
# 2. Ensure the Cloud Run service account has access to:
#    - Google Sheets API
#    - Vertex AI API
# 3. DO NOT set GOOGLE_SERVICE_ACCOUNT_JSON (use ADC instead)
#
# For local development:
# 1. Create a .env file from this template
# 2. Set GOOGLE_SHEET_ID
# 3. Set GOOGLE_SERVICE_ACCOUNT_JSON with your service account JSON
# 4. Or run: gcloud auth application-default login
